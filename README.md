# Curriculum Vitae for Nguyễn Hồng Quân

A CV written in LaTeX using the [moderncv](http://www.ctan.org/pkg/moderncv) class.

## How to compile

- Install `texlive-luatex`, `texlive-latex-extra`, `texlive-fonts-extra` packages in Ubuntu.

- Run `make`.
